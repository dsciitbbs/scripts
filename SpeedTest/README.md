Simple snippet to test your connection speed using fast.com!

Note: You can use chrome.sh in case you are on ubuntu, but in case of Windows, just check the version of chrome (use chrome://version) and download the appropriate verison of chromedriver  
Also, don't forget to change the line inside cli.py to point to that executable